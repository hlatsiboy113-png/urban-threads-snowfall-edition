# Firebase Admin SDK credentials
# Option A: Path to service account JSON file (recommended for local dev)
GOOGLE_APPLICATION_CREDENTIALS=scripts/service-account.json

# Option B: Raw JSON string (useful for CI/CD — paste entire JSON, no quotes needed)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
